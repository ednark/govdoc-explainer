{
  "name": "govdoc-explainer",
  "version": "2.0.0",
  "description": "Pre-process government documents into searchable, LLM-summarized static HTML",
  "private": true,
  "scripts": {
    "build:python": "python -m govdoc_explainer",
    "build:search": "npx pagefind --site . --output-dir assets/pagefind",
    "build": "npm run build:python && npm run build:search",
    "serve": "npx pagefind --site . --serve",
    "test": "pytest"
  },
  "devDependencies": {
    "pagefind": "^1.1.0"
  }
}
